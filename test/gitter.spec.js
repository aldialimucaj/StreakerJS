import Gitter from '../lib/gitter';
import chai from 'chai';

const expect = chai.expect;
chai.should();

describe('Gitter', () => {
    describe('init', () => {
       it('inits the repo', () => {
           expect(1).to.be.a('number');
       });
    });
});